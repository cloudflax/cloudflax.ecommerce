services:
  app:
    build:
      target: deps
    command: sh -c "pnpm install --frozen-lockfile && pnpm dev"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      WATCHPACK_POLLING: "true"
